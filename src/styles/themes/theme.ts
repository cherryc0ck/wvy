import { colors, zIndex, flex, typo, media, a11y } from "./foundations";
import { button, themeMode } from "./components";

export const theme = {
  themeMode,
  button,
  zIndex,
  flex,
  colors,
  typo,
  media,
  a11y,
};
