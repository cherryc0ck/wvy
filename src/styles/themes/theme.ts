import { colors, zIndex, flex, typo, responsive, a11y } from "./foundations";
import { button } from "./components";

export const theme = {
  button,
  zIndex,
  flex,
  colors,
  typo,
  responsive,
  a11y,
};
