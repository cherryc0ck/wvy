import { colors, zIndex, flex, typo, media, a11y } from "./foundations";
import { button } from "./components";

export const theme = {
  button,
  zIndex,
  flex,
  colors,
  typo,
  media,
  a11y,
};
